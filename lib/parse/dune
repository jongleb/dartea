(ocamllex
 (modules lexer))

(menhir
 (flags --strict --dump --explain)
 (modules parser))

(library
 (name parse)
 (libraries menhirLib frontend data non_empty_list reporting)
 (flags
  (-warn-error -A+8))
 (preprocess
  (pps ppx_jane ppx_deriving.show ppx_deriving.make)))
