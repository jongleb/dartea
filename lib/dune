(ocamllex
 (modules lexer))

(menhir
 (flags --strict --dump --explain)
 (modules parser))

(library
 (name dartea)
 (libraries menhirLib)
 (flags
  (-w -40 -w +26))
 (preprocess
  (pps ppx_jane ppx_deriving.show)))
