-- NAMING ERROR ------------------------------------------------------- Main.elm

I cannot find a `Paint.shown` variable:

1| xs = Paint.shown
        ^^^^^^^^^^^

I cannot find a `Paint` import. These names seem close though:

    sin
    xor
    tan
    sqrt

Hint: Read <https://elm-lang.org/0.19.1/imports> to see how `import`
declarations work in Elm.
